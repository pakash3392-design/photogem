# Get a free key at https://auth.pollinations.ai (no credit card needed).
# This is what actually authenticates image-editing requests -- Pollinations'
# edit endpoint returned a 401 without one, so this is required, not optional.
POLLINATIONS_API_KEY=
